class Question {
  String questionText;
  bool questionAnswer;

  Question({String userInputQuestion, bool userInputAnswer}) {
    questionText = userInputQuestion;
    questionAnswer = userInputAnswer;
  }
}
